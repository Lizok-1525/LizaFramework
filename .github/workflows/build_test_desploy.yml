name: 🚀 FTP Deploy

on:
  push:
    branches:
      - main  # Se ejecuta cuando hay cambios en la rama principal

jobs:
  deploy:
    name: 📂 Subir archivos por FTP
    runs-on: ubuntu-latest

    steps:
    - name: 🛒 Obtener el código del repositorio
      uses: actions/checkout@v3

    - name: 📦 Instalar LFTP
      run: sudo apt update && sudo apt install -y lftp

    - name: Crear carpeta www/
      run: mkdir -p LizaFramework

    - name: 📤 Subir archivos al servidor FTP
      run: |
        lftp -e "
        set ftp:ssl-allow no;
        open -u ${{ secrets.FTP_USER }},${{ secrets.FTP_PASSWORD }} ${{ secrets.FTP_HOSTNAME }};
        lcd dist/  # Cambia al directorio local donde están los archivos a subir
        cd /web/  # Cambia al directorio remoto donde quieres subir los archivos
        mput -O /web/ -c
        bye
        "